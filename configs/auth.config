require("dotenv").config();
const crypto = require("crypto");

const secretMaterial = process.env.JWT_SECRET || process.env.MONGODB_URL;

module.exports = {
    secret: secretMaterial
        ? crypto.createHash("sha256").update(`enterpriseflow-jwt:${secretMaterial}`).digest("hex")
        : crypto.randomBytes(64).toString("hex")
};
